# PC Screw Detection Using YOLO
Using transfer learning to train existing YOLO models for small object detection task, specifically detecting pc screws. 

## Project Structure
After cloning the project you should see models and src 
When cloning this project, you are expected to create a datasets directory that includes a data.yaml. The organisation of the data inside the datasets directory is derived from data.yaml. 

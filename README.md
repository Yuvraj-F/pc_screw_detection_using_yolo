# pc_screw_detection_using_yolo
Using transfer learning to train existing yolo models for small object detection task, specifically detecting pc screws. 

## Project Structure
All modules expect datasets, models, and src directories in the top level directory. When cloning this project, you are expected to create a datasets directory that includes a data.yaml. The organisation of the data inside the datasets directori is derived from data.yaml. 
